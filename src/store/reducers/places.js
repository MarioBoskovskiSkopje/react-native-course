const initalState = {
  places: [],
  selectedPlace: null,
}

const reducer = (state = initalState, action) => {
  switch (action.type) {
    case value:
      break

    default:
      return state
  }
}

export default reducer
